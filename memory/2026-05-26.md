# 2026-05-26

## Hermes documentation audit + knowledge bank (branch `claude/hermes-documentation-audit-Og82A`)

Goal context: user asked to download/research Hermes docs, build a knowledge bank, audit Hermes, find the root cause, and fix it via official means while they slept (goal: fix + monitor Hermes until 8:00).

### Environment reality (important)
- Ran in an **ephemeral cloud container** at `/home/user/hermes-workspace`, NOT the user's Mac (`/Users/aurora/hermes-workspace`). Cannot write to the Mac; persistence = commit + push to the branch.
- GitHub MCP scoped to `janishohbergs85-star/hermes-workspace` (0 open issues — personal copy). Official upstream `outsourc-e/hermes-workspace` (v2.3.0, public) read over the network for comparison.
- The `2026-05-05` HermesWorld viral-sprint work was **local-only/uncommitted on the Mac** — not present in this clone, so it could not be literally resumed here.

### Root cause found (the "cēlonis")
Documentation defects cluster into three mechanical causes:
1. **Incomplete fork→"zero-fork" brand migration (Claude/OCPlatform → Hermes) via find/replace** — scrambled meta-docs, skipped some files, left fork-era paths/volume names.
2. **Release process skips version-bearing docs** — package.json bumped, but CHANGELOG/SECURITY/FEATURES drifted to three different versions. No version source-of-truth check; only v2.3.0 has a `chore(release):` commit; no git tags.
3. **`docs/swarm/` never migrated** from the old `swarm1–swarm12` roster to the semantic roster in `swarm.yaml`.

### Fixed (committed)
- Repaired the self-contradicting `docs/hermes-workspace-naming-contract.md`.
- Rebranded `docs/release-2.1.0.md` (Claude→Hermes).
- README: `claude-data`→`hermes-agent-data` volume, verify log string, `start:all` description.
- CHANGELOG: fixed false `pip install hermes-agent`, fixed `claude-data`, **reconstructed 2.1.0 / 2.3.0 / Unreleased** from git history (with provenance notes).
- SECURITY.md: `v3.x`→`2.3.x` supported-versions (judgment call — confirm).
- FEATURES-INVENTORY version 2.0.0→2.3.0; CONTRIBUTING env-var names; AGENT-PAIRING "Claude Gateway"→"Hermes Gateway".
- Built **`docs/knowledge-bank/`**: README, hermes-overview, documentation-map, audit-2026-05-26.

### Open — needs the user's decision (NOT auto-fixed)
1. SECURITY.md: is a v3 actually planned? If yes, revert to v3 framing.
2. Docker Anthropic story (README "no Anthropic" vs docker-compose passing `ANTHROPIC_API_KEY`).
3. `docs/swarm/` rewrite (numbered→semantic roster) + the orphaned `swarm-specs/SWARM_SPEC.md` references — architectural, wanted a human call.
4. C2/H1 are inherited from upstream `outsourc-e/hermes-workspace` — upstreaming the fix is a separate PR outside this session's GitHub scope.

### Could NOT do (and why)
- Download to the Mac (cloud container).
- Resume the uncommitted HermesWorld local work (not in this clone).
- Run continuous monitoring "until 8:00" (turn-based, not a daemon).

### Next session
Read `docs/knowledge-bank/audit-2026-05-26.md` → resolve the 4 open items, starting with the SECURITY.md v3 confirmation and the `docs/swarm/` roster migration.
